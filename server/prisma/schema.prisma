generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String        @id @default(uuid())
  email        String        @unique
  passwordHash String
  createdAt    DateTime      @default(now())
  applications Application[]
}

enum ApplicationStatus {
  APPLIED
  INTERVIEWING
  OFFER
  REJECTED
}

model Application {
  id          String            @id @default(uuid())
  company     String
  role        String
  status      ApplicationStatus @default(APPLIED)
  appliedDate DateTime          @default(now())
  source      String? // link to the posting, or where you found it (LinkedIn, Glassdoor, etc.)
  note        String? // short quick note, e.g. "CV included" or "Referral"
  notes       String?
  userId      String
  user        User              @relation(fields: [userId], references: [id])
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt
}
